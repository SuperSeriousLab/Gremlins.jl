# Gremlins.jl — General Registry Release Checklist

AutoMerge requirements verified for version `0.1.0`.

## Package identity

- [x] **Package name >= 5 characters** — "Gremlins" (8 chars). PASS.
- [x] **UUID present** — `8bb2f624-ef2f-4048-8713-dbabdf2ef7e4`. PASS.
- [x] **Version** — `0.1.0`. Correct format. PASS.
- [x] **Authors field** — `["John Cobbler <jenda.sevcu@seznam.cz>"]`. PASS.

## Compat entries (AutoMerge requires compat for ALL direct deps)

- [x] `julia = "1.10"` — PASS.
- [x] `JuliaSyntax = "0.4, 1"` — PASS.
- [x] `SHA = "0.7.0"` — PASS.
- [x] `Base64 = "1"` — **Added in M3a** (Base64 is a stdlib; was missing before; AutoMerge fails without it). PASS.

Note: `Test` is in `[extras]` / `[targets]` only (not `[deps]`), so no compat entry
needed for it. `Distributed` is not a direct dependency. No other stdlib imports
present in src/ at time of writing.

## Source requirements

- [x] **tests pass** — `Pkg.test()` exits 0; 389 tests (M0-M2) + M3a CLI tests. PASS.
- [x] **LICENSE file** — `LICENSE` (MIT, copyright 2026 John Cobbler). PASS.
- [x] **README.md** — Present with usage examples, benchmark numbers, limitations. PASS.
- [ ] **Public GitHub repo** — Required for registration. See M3b.

## AutoMerge compliance

- [x] No name collision — "Gremlins" not in General registry as of 2026-06-05
  (check again at registration time: `using Pkg; Pkg.Registry.update(); Pkg.pkg"add Gremlins"`)
- [x] Version starts at `0.x` — `0.1.0`. AutoMerge allows first registration without
  a tag on an existing release (tag required for subsequent versions).
- [x] No breaking compat lowering from a previous registration — first registration.

## What M3b will do

1. **Create public GitHub repo** — decision for user:
   - Option A: under `SuperSeriousLab` GitHub org (existing org, consistent with other projects).
   - Option B: new `JuliaTesting` org (higher adoption signal; pitch first, transfer after acceptance).
   - **Recommendation**: start under `SuperSeriousLab`, transfer to `JuliaTesting` after org pitch.

2. **Tag v0.1.0** — `git tag -a v0.1.0 -m "Initial release"`; push tag to public GitHub.

3. **Register** — In the Julia REPL with `LocalRegistry.jl` or via JuliaRegistrator bot:
   ```
   @JuliaRegistrator register
   ```
   from a GitHub issue or PR on the package repo. JuliaRegistrator opens a PR on
   `JuliaRegistries/General`. AutoMerge runs within ~1-3 days if all checks pass.

4. **Post ANN** — see `docs/drafts/ann-discourse.md`. Post AFTER AutoMerge confirms
   (not before — avoids "where is it?" confusion if registration is delayed).

5. **JuliaTesting pitch** — see `docs/drafts/juliatesting-pitch.md`. Open a GitHub
   issue on JuliaTesting org after ANN post.

## What to verify after registration

- `pkg> add Gremlins` installs cleanly in a fresh Julia environment.
- `pkg> test Gremlins` passes all 389+ tests.
- Pkg resolver can satisfy compat: test with Julia 1.10 and Julia 1.11 (latest stable).
- Documentation accessible (auto-generated by Documenter.jl if wired; not required for M3b).
- Badge URLs in README.md updated from placeholders to real CI/registry badges.

## Known gaps to fix before 1.0

- Documenter.jl integration (API docs auto-gen) — not present yet.
- TestItemRunner.jl integration for per-item coverage — design question open from M0.
- Equivalent-mutant heuristics — documented as known noise.
- `budget` parameter for time-capped exploratory runs — API exists, not wired.
