# Locally-built AST complexity binary — rebuilt lazily by score_complexity.sh
bin/

# Audit archives — kept locally as evidence, not committed
archive_pre_ast/
archive_pre_gremlins/

# Local-only critic shadows (SecondThoughts integration)
certified/.second-thoughts/

# Per-package gremlins mutation cache (regenerated by certify_t4_mutation.sh)
.cache/
